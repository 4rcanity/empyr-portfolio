---
import Page from '../../../templates/hammam/Page.astro';
import { hammamConfig } from '../../../data/hammam-config';
import { toSiteConfig } from '../../../data/nl-demo';

export function getStaticPaths() {
  return [{ params: { lang: 'nl' } }];
}
---

<Page config={toSiteConfig(hammamConfig)} lang="nl" />
