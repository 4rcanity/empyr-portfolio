---
import Page from '../../../templates/astex/Page.astro';
import { astexConfig } from '../../../data/astex-config';
import { toSiteConfig } from '../../../data/nl-demo';

export function getStaticPaths() {
  return [{ params: { lang: 'nl' } }];
}
---

<Page config={toSiteConfig(astexConfig)} lang="nl" />
